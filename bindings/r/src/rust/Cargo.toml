[package]
name = "nameparser"
# NOT "0.0.0": the core crate (../../../../crates/nameparser) is ALSO named
# "nameparser" and ALSO pinned at version 0.0.0 (repo convention for unpublished
# crates). Two packages with the identical (name, version) pair from different
# paths cannot coexist in one Cargo lockfile — "error: package collision in the
# lockfile" — so this crate (which, uniquely among the bindings, must share the
# core's exact name for R's `useDynLib(nameparser)`/import-name conventions) is the
# one that has to take a different version. The R-facing version lives in
# DESCRIPTION's `Version: 0.0.0.9000` (Step 6) regardless; this is purely to
# satisfy Cargo.
version = "0.0.1"
edition = "2021"

[lib]
crate-type = ['staticlib', 'lib']
name = 'nameparser'

# Generated by `rextendr::use_extendr()`: a small helper binary that (via the metadata
# `extendr_module!` embeds in the lib target) regenerates ../R/extendr-wrappers.R at
# build time. Invoked by src/Makevars.in as `cargo run --bin document ...` on every
# build, so this target must stay declared even though Task 1's brief Cargo.toml
# (written before this repo's toolchain spike ran) omitted it — dropping it breaks
# `rextendr::document()`/`devtools::document()` (see Task 1 report: rextendr 0.5.0's
# `document()`/`register_extendr()` are deprecated no-ops that delegate to
# `devtools::document()`, which drives wrapper regeneration through this exact Makevars
# recipe, not through any roxygen2 roclet).
[[bin]]
name = 'document'
path = 'document.rs'

[dependencies]
extendr-api = "0.9"
serde_json = "1"
# Aliased to `nameparser_core` (Cargo's `package =` rename key), NOT a plain
# `nameparser = { path = ... }` key as crates/nameparser-py/Cargo.toml uses for the
# same core crate: unlike the PyO3 binding, this package also has a `[[bin]]
# document` target (generated by `rextendr::use_extendr()`, see its comment above)
# that Cargo auto-links against this package's OWN `[lib]` target — which is ALSO
# named "nameparser" (required for R's `useDynLib(nameparser)`). With the core
# crate ALSO keyed "nameparser", the `document` bin ends up with two different
# `.rlib`s both bound to the extern name `nameparser`, which rustc rejects as
# `error[E0464]: multiple candidates for `rlib` dependency `nameparser` found` — a
# *different* error from lib.rs's own-name ambiguity (E0659) a leading `::` fixes;
# E0464 is not fixable by path-qualifying the reference at the call site (verified:
# `::nameparser::get_nameparser_metadata()` in document.rs still hits it), only by
# giving the two same-named crates distinct extern names. Renaming the dependency
# here is the smaller change (one file) vs. renaming this package's own `[lib]
# name` (which several other generated files key off of). lib.rs therefore refers
# to the core as `::nameparser_core::...`, not `::nameparser::...`.
nameparser_core = { package = "nameparser", path = "../../../../crates/nameparser" }

# Detach from the repo's Cargo workspace so R's Makevars can build this crate
# standalone (the root workspace lists only crates/* as members).
[workspace]
