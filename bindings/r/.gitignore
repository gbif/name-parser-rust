src/rust/vendor
src/Makevars
src/Makevars.win

# Artifacts of the CRAN packaging step (scripts/build-r-tarball.sh). It works in a staging copy
# under target/, so these should never appear here -- but running `rextendr::vendor_crates()` or
# the bundling step by hand in this directory would land them in the checkout.
src/rust/vendor.tar.xz
src/rust/vendor-config.toml
src/rust/nameparser-core/
